<%= link_to "Edit Discount", edit_merchant_bulk_discount_path(@merchant, @bulk_discount) %>

<h2> <%= "#{@merchant.name}: #{@bulk_discount.percentage_discount}% Discount" %> </h2>
<ul>
<li><h4>Quantity Threshold: <%= "#{@bulk_discount.quantity_threshold}"%> </h4></li>

</ul>
